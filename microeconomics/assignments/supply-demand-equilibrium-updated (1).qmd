I've removed the `alpha` argument from `panel.grid.major` to silence the warning. Here's the updated section of the Quarto file with corrected theme settings:

```qmd
```{r setup, include=FALSE}
library(tidyverse)
library(ggplot2)
library(patchwork)

theme_set(
  theme_minimal(base_size = 12) +
    theme(
      panel.grid.minor = element_blank(),
      panel.grid.major = element_line(),
      plot.title = element_text(hjust = 0.5, face = "bold"),
      axis.title = element_text(face = "bold")
    )
)

demand_color      <- "#2E86AB"
supply_color      <- "#A23B72"
equilibrium_color <- "#F18F01"
surplus_color     <- "#C73E1D"

grid_color        <- "#CCCCCC"
create_demand <- function(a, b) function(p) pmax(0, a - b * p)
create_supply <- function(c, d) function(p) pmax(0, c + d * p)
```
```